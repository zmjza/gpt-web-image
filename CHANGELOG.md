# Changelog

## 0.4.0 - 2026-08-24

- 修复 ChatGPT 新模型能力菜单：混合高/中/极速文本的父菜单不再被误认成具体模型；支持当前“推理强度”本地化文案、直接能力 slider 和嵌套 Radix 子菜单，模型仍须明确验证后才允许提交。
- 图生图优先使用当前网页唯一的 `upload-photos-input` 专用照片入口；只有旧页面不存在该入口时才回退到 Composer 表单内唯一图片 input，避免把通用文件入口或旧草稿附件当成本轮上传。
- 发送按钮点击后若提示词、用户回合和会话链接三项均证明未派发，则在同一提交尝试内使用一次键盘 Enter 回退；任何已有提交迹象都会禁止回退，继续防止重复生成。
- 媒体查看器唯一“保存”按钮的 hydration 等待从 5 秒扩展到有界 15 秒；已确认提交但交付失败的任务可复用稳定会话和助手回合恢复下载，不重新发送提示词。
- Skill 增加 `$liran-image2`/`image2`/`gpt-image-2` 显式路由让位，并明确 `MODEL_SELECTION_UNCERTAIN` 不等于登录失效或额度耗尽，禁止盲目重试。
- 更新 `nanoid` 传递依赖到 `3.3.18`，官方 npm audit 恢复为 0 vulnerabilities。
- 真实图生图任务 `task_mt78uzxk_xu73ll7q` 使用用户上传参考图，选择 `GPT-5.6 Sol 高`，附件、提交、稳定会话、用户/助手回合、唯一媒体卡和下载事件证据完整；恢复交付 1 张 `1254×1254 PNG`，SHA-256 为 `8bfa2cc17b6365cfafa5e3ff049d39b094ac742c83133dabc60ce5cbf3ca4fc1`。
- 自动化验证：typecheck、完整测试 `172/172`、集成测试 `28/28`、build、官方 npm audit、Skill 快速校验和 `git diff --check` 均通过；用户级安装器返回 `action=upgraded`，源码 Skill 与安装目录内容一致，doctor 确认 Chrome、Profile 和输出目录可用。
- Windows 验证使用原生路径分隔符定位递归任务文件，并为独立 Node 队列子进程提供有界 5 秒启动预算，避免把 runner 调度和路径表示差异误判为业务失败。
- 已知限制：ChatGPT 实际返回 1254×1254，并非原生 4K；本工具不进行本地伪造放大。网页可见语义仍可能变化，结构或证据不明确时继续安全停止。

## 0.3.0 - 2026-08-07

- 明确区分文生图 `generate`、本地参考图生图 `edit --reference` 和网页历史结果图改图 `refine`；缺少或混用来源时安全拒绝提交。
- 每次网页提交前按 `GPT-5.6 Sol 高`、`GPT-5.6 Sol 中`、`极速` 自动选择并验证模型；三者明确不可用时返回 `单日生图已达限额，暂时不可生图。`。
- 增加本地附件上传后的网页可见性、数量、文件名和大小校验；增加当前用户回合、紧邻助手回合、唯一媒体卡、原图下载资源和 SHA-256 的交付 provenance。
- 增加按 Profile 隔离的跨进程持久化 FIFO 队列，避免多个 Codex 对话同时操作同一网页 Composer 时任务和图片串扰。
- `--ratio` 进入网页提示词并做结果方向保护；浏览器在任务证据和结果落盘完成前保持运行，后台使用项目专用最小化 Chrome。
- 真实 ChatGPT 验收已覆盖文生图、图改图、本地参考图生图和同 Profile 双进程 FIFO；各任务的提示词指纹、模型、会话/回合、媒体卡、下载事件、原图和 SHA-256 均分别落盘，未发生串图。
- 修复提交后从历史侧栏误取旧会话 URL、查看器保存按钮延迟挂载、媒体懒加载和 macOS 页面标题被 SPA 重写造成的恢复失败；恢复观察始终禁止重复提交。
- 自动化验证：typecheck、完整测试 `166/166`、build 和 `git diff --check` 已通过。
- 已知限制：网页模型名称、附件证据和原图下载入口依赖 ChatGPT 当前可见语义结构；图生图网页实际返回 `941×1672` 竖屏 PNG，并非原生 4K，因此不宣称 4K 成功，也不进行本地伪造放大。

## 0.2.13 - 2026-08-04

- 修复管理服务并发检查同一 Profile 时将 `BROWSER_LEASED` 冲突错误映射为 500 的问题，现在返回可识别的 409 冲突并保持敏感信息脱敏。
- 增加 BrowserLease 并发冲突 API 回归测试，验证一个检查成功、并发请求明确得到 `BROWSER_LEASED`。
- 本版仍不创建 Tag、Release、安装包或执行 T42。

## 0.2.12 - 2026-08-04

- 修复管理服务重启后把已退出的专用浏览器继续显示为“运行中”的问题：仅当 BrowserLease 文件对应的进程仍存活时保留 `open` 状态，否则启动时恢复为 `closed`。
- 增加陈旧浏览器状态回归测试；不清理 Profile 内容、不改动任务占用状态，仍保持专用 Profile 永不自动删除。
- 本版仍不创建 Tag、Release、安装包或执行 T42。

## 0.2.11 - 2026-08-04

- 为 macOS 专用窗口标题定位增加有界轮询，覆盖 Chrome 标签标题传播竞态；定位失败仍会关闭专用进程并释放锁。
- 图片管理读取所选 Profile 的索引状态并展示权限、缺失、损坏、读取失败和符号链接问题，不再把权限错误误显示为空 Profile。
- 补录真实未登录 Profile 的 `LOGIN_REQUIRED -> headed handoff` 探针、真实 ChatGPT 页面受控结构冲突和隔离目录 `PERMISSION_DENIED` 证据；自然登录失效接管和自然网页改版仍不伪造为通过。
- 本版仍不创建 Tag、Release、安装包或执行 T42。

## 0.2.10 - 2026-08-04

- 修复 macOS 台前调度下 `--start-minimized` 仍可能显示普通窗口的问题：后台任务现在通过唯一页面标记只隐藏对应的专用 Chrome 窗口，不影响用户日常 Chrome；Windows/其他平台使用 CDP 窗口最小化。
- 增加跨平台窗口生命周期回归测试，并完成真实魏邦 Profile 的无提交窗口隐藏复测；本版仍不创建 Tag、Release、安装包或执行 T42。

## 0.2.9 - 2026-08-04

- 修复提交后中断窗口把临时 `WEB:` 会话地址写入恢复锚点的问题；恢复审计现在只接受稳定 ChatGPT UUID 会话，临时地址安全停在 `result_uncertain`，禁止重提。
- 增加稳定会话 URL 回归测试，并完成一次真实 macOS 单图生图、原图下载、解码和 Codex 实时回显复测；本版仍不创建 Tag、Release、安装包或执行 T42。

## 0.2.8 - 2026-08-04

- 稳定 Windows 并行 Chrome 夹具中的 T48 hydration 回归：将测试观察预算从 2 秒调整为有界的 5 秒，保留生产资格检查的 15 秒门禁不变。
- 记录 Windows Actions run `30834139133` 的真实失败边界和修复后的本地回归证据；该版本仍不创建 Tag、Release、安装包或执行 T42，Windows x64 用户验收继续待用户确认。

## 0.2.7 - 2026-08-04

- 修复 768–1023px 平板管理页顶部导航的 flex 收缩问题，导航标签保持单行可读，刷新操作使用固定 40×40 图标区并收敛低优先级账户工具。
- 新增平板导航 UI 回归测试，并完成 1280×1024、768×900、390×844 三视口截图、尺寸、控制台和网络复测。
- 本版仅完成源码与验证，不创建 Tag、Release、安装包或执行 T42；Windows CI 和 Windows x64 用户验收仍按门禁推进。

## 0.2.6 - 2026-08-03

- 修复真实 ChatGPT 中文账户入口通过角色语义、`aria-label` 或无空格本地化文本暴露时的 Plus/Pro/Go 会员识别。
- 管理页面资格检查现在等待稳定 composer；生图控件暂未完成 hydration 时不再把已识别的合格会员误判为不合格。
- 增加魏邦 Plus、里燃未登录和延迟 hydration 的回归测试，并保留未登录/验证/技术失败的启用阻止边界。
- 记录真实连续修改在 `SUBMISSION_UNCERTAIN` 后只读确认并安全恢复的证据；本版未发布，Windows x64 用户验收和 T42 发布门禁仍未执行。提交 `f7cf134a2bd7c0d410b31dd154ba98109d949b6f` 对应 Windows Actions run `30821377390` 已绿色。

## 0.2.5 - 2026-08-03

- 修复 ChatGPT 中文界面将 `你说：` 和 `展开收起` 展示控件拼入用户回合后造成的 `SUBMISSION_UNCERTAIN` 误判；提交指纹现在只比较去除这些 UI 包装后的真实消息文本。
- 增加该真实网页边界的回归测试；保持 `uncertain` 禁止盲目重提的安全策略不变。
- 补录真实双图歧义选择后的 refine 恢复交付证据；本版对应 Windows Actions run `30816483993` 已绿色，但未发布，Windows x64 用户验收和 T42 发布门禁仍未执行。

## 0.2.4 - 2026-08-03

- 只接受稳定 ChatGPT UUID 会话 URL，拒绝临时 `WEB:` 链接进入任务恢复锚点。
- 专用 Chrome 启动后显式导航到任务 URL，并按助手回合序号等待 DOM 挂载，避免恢复旧标签页或错图。
- 在提交点击边界捕获真实助手 `data-state`，避免跨平台导航时漏掉 `queued` 状态事件。
- 新增真实提交后中断恢复证据；本版未发布，T42 和 Windows x64 用户验收仍未执行。

## 0.2.3 - 2026-08-03

- 修复真实 `resume` 在 ChatGPT 会话 DOM 延迟挂载时过早读取助手回合的问题。
- 恢复 observer 现在按持久化 `assistantTurnOrdinal` 等待原助手回合出现后再继续监控；本版未发布，T42 和 Windows x64 用户验收仍未执行。

## 0.2.2 - 2026-08-03

- 修复 `resume` 对已确认提交任务只输出审计结果、不恢复网页监控的问题。
- 恢复时复用任务绑定的 Profile、原 ChatGPT 会话和助手回合锚点，禁止重复提交，并继续逐图交付剩余图片。
- 增加恢复 observer 集成回归测试；本版未发布，Windows x64 用户验收和 T42 发布门禁仍未执行。

## 0.2.1 - 2026-08-02

- 修复图片筛选无结果时误显示“该 Profile 暂无图片”，现在会与真实空 Profile 状态明确区分。
- 修复 CLI `--version` 硬编码旧版本，改为从 `package.json` 读取当前版本。
- 本版未发布；Windows x64 用户真机验收和 T42 发布门禁仍未执行。

## 0.2.0 - 2026-07-31

- 新增多 Profile 注册表、创建/导入/编辑、唯一启用、Plus/Pro/Go 资格检查、专用 Chrome 租约和任务 Profile 快照绑定。
- 新增默认目录扫描、迁移/保留、完整未加密备份、恢复为新 Profile 和页面专属二次删除确认。
- 新增只监听 `127.0.0.1` 的本地管理 API，完成 Profile、目录、备份、浏览器和图片管理 UI 的真实接线。
- 新增按 Profile 隔离的图片索引、增量扫描、组合筛选、排序、分组、分页、缩略图、详情、原图和导出；生成中/失败任务在无图片文件时仍可见。
- 图片和 `task.json` 扫描均拒绝符号链接越界；管理 API 采用字段白名单、错误脱敏且不提供图片删除能力。
- 将 Stitch 运行时 CDN 资源迁移为本地 Tailwind 和 Font Awesome 构建资产，完成桌面、平板和移动端管理界面。
- 本版未发布；macOS 真实 ChatGPT 资格/浏览器操作、本次 Windows CI 和 Windows x64 用户验收仍需后续实测。

## 0.1.1 - 2026-07-30

- 修复首次 Google/OpenAI 登录被判定为不安全浏览器：可视流程改用正式 Chrome、专用 Profile 和非零回环调试端口。
- 恢复 Chromium 沙箱，移除可视登录流程对 Playwright 自动化启动参数的依赖。
- 修复页面导航瞬间缺少 `document.body` 导致 setup 退出，并收紧 Cloudflare/Turnstile 验证识别，避免普通聊天文字误判。
- macOS 真机已确认专用 Profile Google 登录并由 `setup` 输出 `state=ready`；真实生图与回显验收仍待完成。
