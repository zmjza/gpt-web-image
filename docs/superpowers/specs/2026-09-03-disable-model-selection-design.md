# 禁用模型选择设计

## 目标

`gpt-web-image` 在提交生图请求时不再读取、打开、切换或验证 ChatGPT 模型菜单，直接使用网页当前已选模型提交。

## 实现

- 在公共网页提交入口删除 `selectImageModel` 调用及回调。
- 保留旧任务记录中的 `modelSelection` / `modelSelections` 字段以兼容已有数据，新任务不再写入。
- 不增加开关或新抽象；旧模型选择模块暂保留，但不再处于运行链路。

## 验证

受控页面将所有模型项标记为不可用，旧实现会在提交前失败；新实现必须直接生成成功，且模型菜单始终保持关闭。
