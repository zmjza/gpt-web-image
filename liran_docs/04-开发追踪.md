# 开发追踪

> 本表由 [[03-索引]] 的依赖拓扑推导。T01–T38 已完成实现与验证；当前代码版本为 0.2.13，生命周期修复提交 `81613228a082a10930b01212dfcedd6d17e2ccb8` 和文档收口提交 `692203f1a8a7f17a12180f71c2ac5953f83c6b14` 已推送；对应 Windows Actions run `30943199641`、`30943572476` 均成功。公开日志下载接口要求仓库管理员权限，本轮已通过 GitHub 连接器读取 job 日志；T40 范围内的 macOS 真机、管理页面和图片管理已有证据，4.2 提交前/提交后安全恢复边界已有真实证据，4.3、4.4 按用户决定不纳入本轮验收，真实用户目录破坏性操作仍按安全边界执行；T41/T79/IMG-7 按目标开头的用户决定豁免并默认通过（无真机证据），T42 未执行。
> 常规 `已完成` 门禁要求满足 macOS 真机、Windows CI 和 Windows 用户验收责任；本轮目标开头已明确跳过 Windows x64 用户真机，故当前收口按“范围豁免（无真机证据）”记录，不将其解释为 Windows 用户真机通过。

> 历史登录复测曾发现专用 Profile 目录未被删除但会话不可用；本轮已重新用 `doctor --json` 和管理服务只读检测确认专用目录、归属标记、`logged_in / plus` 与后台资源清理均正常。该历史问题仍保留在下方问题回写中，不把普通 Chrome 登录当作专用 Profile 登录。

## 开发顺序

1. 建立项目骨架、Skill 元数据、配置、路径、数据与事件契约。
2. 实现持久化、脱敏、清理、锁与任务编排等不依赖网页的底层能力。
3. 实现浏览器 Profile、首次登录、后台/可视切换。
4. 实现 ChatGPT 网页语义适配、提交幂等和回复绑定。
5. 实现生成状态机、DOM 观察、多信号判定和结构异常升级。
6. 实现图片发现、原图下载、校验、预览和 JSONL 逐张回显。
7. 实现恢复、完整 CLI、Skill 编排与跨平台安装。
8. 完成可控网页夹具、内部全量测试和 Windows CI。
9. 进入第二次 `/goal`：macOS 真机、Windows 用户真机验收、问题回写和版本收口。

## 微观任务清单

| ID | 模块 | 前置 | 目标文件（规划） | 先写的失败测试 | 最小实现 | 验证类别 | 完成证据 | 状态 | 验收责任 |
|---|---|---|---|---|---|---|---|---|---|
| T01 | 项目基础 | 无 | `package.json`、`tsconfig.json`、`.gitignore`、`src/cli.ts` | `tests/smoke/project-layout.test.ts` 证明脚本/编译入口缺失 | 仅建立 npm/TypeScript/Node test 骨架和忽略运行数据规则 | install/typecheck/test | 本地 install、build、typecheck、17 个测试通过；CI 待后续 | 内部测试通过 | Codex 内部 |
| T02 | [[Skill元数据与自动触发]] | T01 | `.agents/skills/gpt-web-image/SKILL.md`、`agents/openai.yaml` | 正/负触发提示集无法找到 Skill 或误触发 | 写最小元数据、隐式策略、默认提示与脚本入口说明 | skill metadata/unit | 本地元数据测试覆盖显式、隐式和负向边界 | 内部测试通过 | Codex 内部 |
| T03 | [[显式调用与参数解析]] | T01 | `src/input/parse-request.ts`、`tests/input/parse-request.test.ts` | generate/edit/refine、数量、比例、附件解析失败 | 返回版本化 NormalizedRequest，不启动浏览器 | unit/typecheck | 正常、边界、缺失参考图测试通过 | 内部测试通过 | Codex 内部 |
| T04 | [[歧义确认与安全提交]] | T03 | `src/input/ambiguity.ts`、`tests/input/ambiguity.test.ts` | 多图目标/数量冲突仍被判可提交 | 返回唯一问题或 executable=true，提交前硬门禁 | unit | 多图歧义和数量冲突测试通过，未创建网页提交 | 内部测试通过 | Codex 内部 |
| T05 | [[配置加载与默认值]] | T01 | `src/config/schema.ts`、`src/config/load.ts` | 非法数量/超时/未知字段未拒绝 | 实现默认值、优先级、范围校验和安全字段拒绝 | unit/contract | 默认值、未知字段、敏感字段和范围测试通过 | 内部测试通过 | Codex 内部 |
| T06 | [[系统目录与路径规范]] | T05 | `src/platform/paths.ts`、`tests/platform/paths.test.ts` | 中文、空格、Windows 保留名或越界路径失败 | 用 Node Path 生成平台目录和安全文件名 | unit/platform simulation | 中文空格、保留名和越界路径模拟测试通过；CI 待后续 | 内部测试通过 | Codex + Windows CI |
| T07 | [[Chrome探测与能力检查]] | T05,T06 | `src/platform/chrome.ts`、`src/commands/doctor.ts` | 未配置 Chrome、错误架构和不可写目录未分类 | 先配置后标准路径探测，输出脱敏 doctor JSON | unit/platform simulation | 显式缺失路径和 doctor 脱敏测试通过；真实平台探测待后续 | 内部测试通过 | Codex + Windows CI |
| T08 | [[JSONL事件协议]] | T01 | `src/events/schema.ts`、`src/events/writer.ts` | 非 JSON 混入 stdout、seq 重复、敏感字段泄露 | 单行 JSON writer、递增 seq 和 schema 校验 | unit/contract | 19 个测试中的事件协议测试通过，序号递增、image_ready 字段完整且敏感消息被拒绝 | 内部测试通过 | Codex 内部 |
| T09 | [[TaskRecord与任务ID]] | T05,T06 | `src/tasks/model.ts`、`src/tasks/id.ts` | taskId 非路径安全、状态/字段不满足 schema | 实现 TaskRecord 工厂和安全唯一 ID | unit/contract | TaskRecord 字段、状态、路径安全和非法 ID 测试通过 | 内部测试通过 | Codex 内部 |
| T10 | [[task-json持久化]] | T09 | `src/persistence/task-store.ts` | 中途写入导致损坏、未知 schema 被误读 | 同目录临时写入、关闭后原子替换、版本校验 | unit/fault injection | 原子替换、失败保留可信文件、未知 schema 拒绝测试通过 | 内部测试通过 | Codex 内部 |
| T11 | [[日志脱敏与诊断保留]] | T06 | `src/diagnostics/redact.ts`、`src/diagnostics/store.ts` | Cookie/token/Authorization 出现在日志快照 | 实现键名、头、URL 参数和文本模式脱敏 | security/unit | Cookie/token/Authorization/URL 查询参数脱敏及 7 天元数据测试通过 | 内部测试通过 | Codex 内部 |
| T12 | [[七天清理与目录边界]] | T06,T11 | `src/diagnostics/cleanup.ts` | 越界符号链接、图片目录或未过期文件被删除 | realpath 根校验、类型白名单、dry-run 与删除报告 | security/unit | 本地清理、dry-run、符号链接和 task.json 边界测试通过；Windows CI run `30547328587` 通过 | 内部测试通过 | Codex + Windows CI |
| T13 | [[单实例锁与残留进程]] | T06,T10 | `src/browser/profile-lock.ts` | pid 复用/活锁/死锁导致误杀或双开 | 原子 lock、ownerToken、进程归属验证和安全释放 | unit/process integration | 活锁拒绝、归属不明锁拒绝、已验证死锁回收测试通过 | 内部测试通过 | Codex + Windows CI |
| T14 | [[串行队列与取消]] | T09,T10,T13 | `src/tasks/queue.ts`、`src/tasks/cancel.ts` | 并发执行、重复取消、取消丢失成功结果 | FIFO 单执行权、持久化取消信号和幂等清理 | unit/integration | FIFO 单执行、排队取消、运行取消信号和幂等测试通过 | 内部测试通过 | Codex 内部 |
| T15 | [[数量记忆与补图]] | T05,T09 | `src/tasks/count-policy.ts` | 首次/记忆/额外图/三轮上限计算错误 | 实现 1–10 规范、明确数量持久化和补图决策 | unit | 1–10、数量记忆、三轮上限及 CLI 补图循环测试通过 | 内部测试通过 | Codex 内部 |
| T16 | [[多图目标选择]] | T03,T09 | `src/tasks/selection.ts` | 多图未指定却自动选第一张 | 单张自动、多张候选、位置词和明确全部解析 | unit | 单张、编号、最后一张、全部和歧义测试通过 | 内部测试通过 | Codex 内部 |
| T17 | [[Profile生命周期]] | T07,T13 | `src/browser/profile.ts` | Profile 误用日常目录或异常关闭不释放资源 | 专用归属标记、持久化 context 和受控关闭 | integration | 专用归属标记、陌生目录拒绝和真实 Chrome 夹具启动通过 | 内部测试通过 | Codex macOS + Windows CI |
| T18 | [[首次可视登录]] | T17 | `src/browser/login.ts`、`tests/fixtures/login-page/` | 仅 URL 跳转就误判登录成功 | 正式 Chrome headed 启动，只以可交互输入区和会话稳定判就绪 | fixture integration | 登录、验证、未知和稳定输入区分类测试通过；macOS 专用 Profile Google 登录与 setup `state=ready` 已验证 | 内部测试通过 | Codex macOS 真机 |
| T19 | [[后台运行与人工接管]] | T17,T18 | `src/browser/handoff.ts` | headless 验证页时重复提交或丢失任务 | 保存任务/URL、关闭后台、同 Profile 可视重开并续接 | fixture integration | taskId/attemptId/chatUrl 保持与禁止重提合同测试通过；真实接管未执行 | 内部测试通过 | Codex macOS 真机 |
| T20 | [[新会话与连续修改]] | T14,T17 | `src/chatgpt/conversation.ts` | 新任务复用旧会话或 refine 丢上下文 | generate/edit 新会话，refine 复用已记录 chatUrl | fixture integration | generate/edit/refine 策略与丢失上下文拒绝测试通过 | 内部测试通过 | Codex 内部 + 真机 |
| T21 | [[语义定位与结构兼容]] | T17 | `src/chatgpt/locators.ts` | CSS 改名后关键控件失效或多候选仍点击 | 角色/名称/关系优先的 locator 适配层和冲突错误 | fixture integration | 可访问语义定位、随机 ID 和多候选冲突测试通过 | 内部测试通过 | Codex + Windows CI |
| T22 | [[提交确认与幂等保护]] | T10,T20,T21 | `src/chatgpt/submit.ts` | 点击超时后重复发送相同提示词 | 基线/attemptId 先落盘，新用户消息证据确认后标 submitted | fixture/fault injection | 已确认/明确未提交/不确定分类及真实 CLI 持久化接线通过 | 内部测试通过 | Codex 内部 + 真机 |
| T23 | [[回复区域绑定与防串图]] | T20,T22 | `src/chatgpt/response-anchor.ts` | 历史图或相邻回复被绑定 | 用用户 turn 邻接关系绑定唯一 assistant turn | fixture integration | 历史图夹具与唯一邻接回复绑定测试通过 | 内部测试通过 | Codex 内部 + 真机 |
| T24 | [[状态机与迁移]] | T08,T09,T23 | `src/monitor/state-machine.ts` | 非法跳转、终态被覆盖、下载前成功 | 显式迁移表、终态保护和证据要求 | unit/property | 合法路径、非法跳转、终态保护和成功证据测试通过 | 内部测试通过 | Codex 内部 |
| T25 | [[DOM观察与活动时钟]] | T23,T24 | `src/monitor/dom-observer.ts` | 无关动画刷新超时或 anchor 导航后失效 | 注入 MutationObserver、相关变化过滤和重绑定 | fixture integration | 相关变化、无关噪声和重绑定时钟测试通过 | 内部测试通过 | Codex + Windows CI |
| T26 | [[多信号判定与超时]] | T24,T25 | `src/monitor/evidence.ts`、`watchdog.ts` | 静止误判成功、429/5xx/超时未分类 | 融合控件、图片、错误、网络旁证并实现三类超时 | fixture/time control | 排队/生成/部分、429/5xx、登录及三类超时测试通过 | 内部测试通过 | Codex + Windows CI |
| T27 | [[结构异常与诊断升级]] | T11,T21,T26 | `src/monitor/structure-error.ts` | 关键元素缺失后仍坐标点击或日志泄密 | 安全停止、诊断请求、可视升级和明确错误码 | security/fixture | 安全停止、结构错误码和诊断脱敏测试通过 | 内部测试通过 | Codex 内部 |
| T28 | [[图片发现与稳定判定]] | T23,T25,T26 | `src/images/discovery.ts` | 占位/历史/零尺寸/抖动资源被计数 | anchor 内基线差集、加载/尺寸/稳定窗口判断 | fixture/time control | 历史、越界、零尺寸、稳定窗口和末批竞态测试通过 | 内部测试通过 | Codex + Windows CI |
| T29 | [[原图下载与临时文件]] | T06,T28 | `src/images/download.ts` | 无下载事件时截图降级或半文件转正 | download 事件优先、页面暴露资源回退、`.partial` 写入 | fixture/download integration | Chrome download 事件、公开资源回退、partial 原子转正和禁截图测试通过 | 内部测试通过 | Codex + Windows CI |
| T30 | [[格式校验与去重]] | T29 | `src/images/validate.ts`、`hash.ts` | HTML/损坏/重复文件被计成功 | 魔数、MIME、真实解码、尺寸、字节和 SHA-256 校验 | unit/integration | sharp 真实解码、尺寸、MIME、损坏拒绝和 SHA-256 测试通过 | 内部测试通过 | Codex + Windows CI |
| T31 | [[PNG预览与输出目录]] | T06,T30 | `src/images/preview.ts`、`output-layout.ts` | 预览覆盖原图或输出越界 | 分目录保存，必要时生成 PNG，保留 originalPath | unit/integration | 分目录、原图不变、PNG 预览可解码和路径边界测试通过 | 内部测试通过 | Codex + Windows CI |
| T32 | [[逐张实时回显]] | T08,T30,T31 | `.agents/skills/gpt-web-image/SKILL.md`、`src/events/image-ready.ts` | 第一张直到终态才输出或同图重复输出 | 每个唯一 ImageResult 立即发 image_ready，Skill 立即渲染 | contract/stream integration | 完整 CLI 夹具证明两条 image_ready 均早于 terminal 且文件存在 | 内部测试通过 | Codex 内部 + 真机 |
| T33 | [[进度节流与最终核对]] | T15,T24,T32 | `src/events/progress.ts`、`reconcile.ts` | 重复刷屏或终态计数与文件不一致仍成功 | 状态/计数节流，终态核对文件、事件、目标和失败 | unit/contract | 重复节流、文件/事件/目标一致性和不确定结果测试通过 | 内部测试通过 | Codex 内部 |
| T34 | [[中断恢复与结果不确定]] | T10,T22,T23,T33 | `src/persistence/recover.ts`、`src/cli.ts`、`src/chatgpt/web-flow.ts` | 中断后一律重提或错误认领历史结果 | 只读恢复审计；证据充分时复用原 Profile/稳定会话/助手锚点重建 observer，否则不确定 | fault injection/fixture/真实 macOS | 提交前、未确认提交、已绑定提交决策及 observer 集成回归通过；真实提交后恢复已通过 | 内部测试通过，真实提交后恢复通过 | Codex 内部 + 真机 |
| T35 | CLI 整合 | T07,T12,T14,T18,T20,T33,T34 | `src/cli.ts`、`src/commands/*.ts` | 命令退出码/事件 stdout/日志 stderr 不符合契约 | 串接 setup/doctor/generate/edit/refine/resume/cancel/cleanup | CLI contract | 命令路由、退出码及 generate 真实 Chrome 夹具端到端测试通过 | 内部测试通过 | Codex + Windows CI |
| T36 | [[用户级安装与升级]] | T02,T35 | `scripts/install.mjs`、`src/commands/install.ts` | 覆盖陌生同名 Skill 或升级丢 Profile | 跨平台安装、归属校验、原子替换和回滚 | install integration | 临时 HOME 安装、归属升级、备份回滚和陌生目录拒绝测试通过 | 内部测试通过 | Codex + Windows CI |
| T37 | [[可控网页夹具]] | T21,T22,T23,T24,T28,T29 | `tests/fixtures/chatgpt-page/**`、`tests/integration/web-flow.test.ts` | 无法确定性重现排队/逐图/失败/改版 | 创建仅测试环境可用的本地状态页面和下载资源 | integration | 本机真实 Chrome 运行排队、逐图、末批稳定和 429 场景通过；新增慢持久化回调回归，证明初始排队态不会因 Windows I/O 延迟丢失；未访问真实账号 | 内部测试通过 | Codex 内部 |
| T38 | [[单元与契约测试]] | T03-T37 | `tests/**/*.test.ts` | 需求矩阵存在未覆盖分支 | 补齐 [[08-测试用例]] 所列 P0/P1 自动化用例 | test/typecheck/security | T01–T38 覆盖矩阵及当前 123 项自动化测试通过；集成套件 12 项通过 | 内部测试通过 | Codex 内部 |
| T39 | [[Windows-CI]] | T36,T37,T38 | `.github/workflows/windows.yml` | Windows 路径/锁/安装/下载未在目标 runner 验证 | `windows-latest` 安装 Chrome/Node 并跑全量非账号测试 | CI/build | 生命周期修复提交 `81613228a082a10930b01212dfcedd6d17e2ccb8` 的 run `30943199641` / job `92106538196` 和文档收口提交 `692203f1a8a7f17a12180f71c2ac5953f83c6b14` 的 run `30943572476` 均全步骤绿色；已读取最新 job 日志，覆盖 Windows Server 2025、Node 22.23.1、npm ci（0 vulnerabilities）、Chrome 探测、typecheck、136/136 测试和 build | Windows CI 通过 | Codex Windows CI |
| T40 | [[macOS真机验收]] | T38,T39 | `liran_docs/09-真机实测.md`、真实输出目录 | 范围内的登录、生成、交付、管理页面和图片管理证据完整 | 按范围执行 Mac setup、1/多图、改图、取消、恢复、管理页面和图片管理 | macOS real E2E | 魏邦 Plus 唯一启用、里燃未登录阻止；真实后台 1 张、2 张、10 张、参考图改图、连续修改、取消、提交前/提交后恢复、3.5 refine、管理页面和 IMG-1–IMG-6 已有证据；MP-2–MP-6 使用隔离一次性 Profile，符合安全边界 | 范围内完成：2.1–2.3、3.1–3.5、4.1–4.2、MP-1–MP-6 和 IMG-1–IMG-6；4.3、4.4 按用户决定不纳入本轮 | Codex 真机 |
| T41 | [[Windows用户真机验收]] | T39,T40 | `liran_docs/09-真机实测.md`、用户验收说明 | Windows 真实 Plus/Codex 链路本轮不执行 | 保留安装入口、最短步骤和预期，未来可按需启动真实验收 | Windows real E2E | 本轮按用户指令豁免，默认通过但无真机证据 | 范围豁免 | 用户真机 |
| T42 | [[发布门禁与文档收口]] | T40,T41 | `package.json`、`CHANGELOG.md`、全部 `liran_docs` | 版本/文档/证据/状态不一致仍准备发布 | 版本递增、说明、完整 diff 与双平台证据核对 | full verification/review | 版本协议、测试、构建、审查、文档和状态全部一致 | 待开发 | Codex + 用户验收 |

## 模块状态看板

| 顺序 | 模块 | 文档 | 状态 | 实测清单 | 备注 |
|---|---|---|---|---|---|
| 1 | 跨平台基础 | [[_02-跨平台基础]] | 内部测试通过 | [[09-真机实测]] | Windows CI 已通过；Windows x64 用户验收本轮按范围豁免 |
| 2 | 持久化诊断 | [[_09-持久化诊断]] | 内部测试通过 | [[09-真机实测]] | 单元、安全和故障注入通过 |
| 3 | 任务编排 | [[_04-任务编排]] | 内部测试通过 | [[09-真机实测]] | 队列、数量、选择合同通过 |
| 4 | 浏览器会话 | [[_03-浏览器会话]] | 内部测试通过 | [[09-真机实测]] | 本地 Chrome 夹具通过；macOS 专用 Profile 登录已验证，真实生成待验收 |
| 5 | Skill 调用 | [[_01-Skill调用]] | 内部测试通过 | [[09-真机实测]] | 触发、参数和实时渲染约定完成 |
| 6 | 网页适配 | [[_05-网页适配]] | 内部测试通过 | [[09-真机实测]] | 可控夹具通过；真实 ChatGPT 未执行 |
| 7 | 生成监控 | [[_06-生成监控]] | 内部测试通过 | [[09-真机实测]] | 状态、活动、错误和超时合同通过 |
| 8 | 图片交付 | [[_07-图片交付]] | 内部测试通过 | [[09-真机实测]] | 下载、真实解码、去重和预览通过 |
| 9 | 实时回显 | [[_08-实时回显]] | 内部测试通过 | [[09-真机实测]] | CLI 夹具中逐张事件早于终态 |
| 10 | 验证发布 | [[_10-验证发布]] | 进行中 | [[09-真机实测]] | T39 当前提交 Windows CI 已通过；T40 范围内已有真实生图/改图/连续修改/取消、提交前后恢复、3.5 refine、管理页面和图片管理证据，4.3/4.4 按用户决定不纳入；T41/T79/IMG-7 按目标豁免并默认通过（无真机证据）；T42 未执行 |

## 状态迁移规则

1. `待开发 → 进行中`：第一次 `/goal` 开始对应任务。
2. `进行中 → 内部测试通过`：实现、类型检查、单元/契约/夹具测试有真实证据。
3. Codex 负责部分：`内部测试通过 → 待实测 → 真机测试通过`。
4. Windows 用户部分：`内部测试通过 → 待用户验收 → 用户验收通过`。
5. 双平台证据、问题回写和文档同步全部收口后，项目才可进入 `已完成`。
6. 任一失败可回到 `进行中`；外部账号、环境或权限真正阻塞时标记 `受阻` 并写清原因。

## 循环依赖检查

未发现设计层循环依赖。Skill 调用依赖任务/事件运行能力，但 Skill 元数据 T02 可先独立建立；最终整合由 T35/T36 完成，未形成实现循环。

## 本轮新增任务拆分：多 Profile 管理系统

第一轮 `/goal` 已完成 T43–T59 的内部实现和自动化验证；修复提交 `8c218e0` 对应 Windows Actions run `30753162951` / job `91510684171` 已提供最新证据。下一门禁仍是按 `09-真机实测.md` 补齐真实 3.5、4.2–4.3 和图片管理真实页面证据，用户只负责 Windows x64 最终验收。

| 编号 | 任务 | 依赖 | 规划文件/模块 | 内部完成标准 | 状态 | 验收影响 |
|---|---|---|---|---|---|---|
| T43 | ProfileRegistry 模型与原子存储 | T05,T10 | `src/profiles/registry.ts` | 版本校验、原子写入、旧配置兼容、安全字段检查 | 内部测试通过 | Mac/CI |
| T44 | 默认目录解析与扫描 | T43 | `src/profiles/directories.ts`、`scanner.ts` | 只扫描受控根目录和有效标记，旧 Profile 自动注册 | 内部测试通过 | Mac/CI |
| T45 | Profile 创建/导入/修改 | T43,T44 | `src/profiles/manager.ts` | 创建标记、显式导入、名称备注更新，拒绝日常 Chrome | 内部测试通过 | Mac/CI |
| T46 | 默认目录迁移/保留 | T43-T45 | `src/profiles/migration.ts` | 预检、冲突停止、复制校验、失败保留源、保留模式 | 内部测试通过 | Mac/CI |
| T47 | 唯一启用与资格状态 | T44,T45 | `src/profiles/manager.ts` | 最多一个 active，任务绑定快照，未达资格禁止启用 | 内部测试通过 | Mac/真机 |
| T48 | GPT 登录和会员检测 | T47,T50 | `src/browser/membership.ts` | Plus/Pro/Go 多信号识别，其他/技术失败阻止 | 真实魏邦网页检测 `logged_in / plus`，里燃 `needs_login`；新增角色/aria-label 账户入口、稳定 composer 等回归，会员测试 6/6、管理服务测试 7/7；0.2.6 最终 CI 已通过 | Mac/真机 |
| T49 | 全局 BrowserLease | T44,T47 | `src/browser/browser-lease.ts` | 全局单 Chrome、人工/任务标签页隔离、归属关闭 | 内部测试通过 | Mac/真机 |
| T50 | 生图任务 Profile 绑定 | T47,T49 | `src/cli.ts`、`src/tasks/model.ts` | generate/edit/refine/resume 绑定 active 快照，无回退 | 内部测试通过 | Mac/CI/真机 |
| T51 | 本地管理服务 API | T43-T49 | `src/manager/server.ts` | 回环监听、规划 API、脱敏响应和错误码 | 内部测试通过 | Mac/CI |
| T52 | HTML 管理页面 UI 壳 | T51 | `src/manager/public/` | 中文列表、表单、弹框、迁移/删除/备份状态和响应式 | 内部测试通过 | UI/真机 |
| T53 | 删除二次确认与自动保护 | T43,T49,T51 | `src/profiles/manager.ts` | 仅页面确认可删，active/占用拒绝，自动路径无删除 | 内部测试通过 | Mac/真机 |
| T54 | 未加密完整备份 | T45,T49 | `src/profiles/backup.ts` | 浏览器关闭后归档全部 Chrome 数据，显示敏感警告 | 内部测试通过 | Mac/CI |
| T55 | 备份恢复为新 Profile | T43,T54 | `src/profiles/backup.ts` | 校验归档、创建新 ID、原 Profile 不覆盖 | 内部测试通过 | Mac/CI |
| T56 | 管理页面接线检查 | T52 | `liran_docs/10-UI壳接入清单.md` | 白名单、局部样式、状态/事件/mock/type 入口齐全 | 内部测试通过 | UI |
| T57 | 单元/契约/安全测试 | T43-T55 | `tests/profiles/**`、`tests/manager/**` | T-MP-01 至 T-MP-20 覆盖，敏感字段和删除边界通过 | 内部测试通过 | CI |
| T58 | Windows 路径与 CI 扩展 | T57 | `.github/workflows/windows.yml` | 中文、空格、迁移、注册表和回环服务在 Windows 通过 | 内部测试通过（修复后 Actions run `30753162951` / job `91510684171`） | Windows CI |
| T59 | 文档和避坑增量回写 | T43-T58 | `liran_docs/**`、`docs/pitfalls/**` | 实际新坑有证据后写回，文档状态与代码一致 | 本轮真实会员检测和中文 UI 误判边界已回写；0.2.6 提交和 Windows CI 证据已同步 | 全部 |
| T60 | macOS 真机管理验收 | T56-T59 | `liran_docs/09-真机实测.md` | MP-1 至 MP-6 在安全允许的隔离数据根完成证据，失败回写复测 | 范围内完成：MP-1 真实页面/API，MP-2/MP-3/MP-5/MP-6 隔离页面，MP-4 隔离临时 Profile 的真实 Chrome 生命周期；真实用户 Profile 未触碰 | macOS 真机 |
| T61 | Windows CI/用户验收准备 | T57-T59 | `liran_docs/09-真机实测.md`、`modules/10-验证发布/Windows用户真机验收.md` | MP-7 CI 证据和 Windows x64 最短步骤准备完成 | MP-7 已由当前 Windows CI 覆盖；Windows x64 用户步骤和反馈模板保留，但本轮按用户指令豁免 | 范围豁免（无真机证据） | Windows CI/用户 |

## 本轮新增任务拆分：图片管理

> T62–T77 已完成第一次 `/goal` 内部实现和页面检查；T78–T79 保持后续 macOS、Windows CI 和 Windows x64 用户验收门禁。

| 编号 | 任务 | 依赖 | 规划文件/失败测试 | 最小实现与完成证据 | 状态 | 验收责任 |
|---|---|---|---|---|---|---|
| T62 | 图片管理导航与视图路由 | T52,T56 | `src/manager/public/app.js`、`tests/manager/ui-shell.test.ts` | 顶部和移动端导航切换唯一 active 视图，不破坏 Profile 页面 | 内部测试通过 | Codex 页面检查 |
| T63 | 单 Profile 选择上下文 | T43,T47,T62 | `src/images/manager-query.ts`、`src/manager/public/app.js` | 未选 Profile 不发起图片请求，选择后只查询一个 `profileId` | 内部测试通过 | Codex + Windows 用户 |
| T64 | Profile 图片根目录解析与安全扫描 | T44,T63 | `src/images/manager-scanner.ts`、`tests/images/manager-domain.test.ts` | 图片与 `task.json` 均拒绝越界和符号链接 | 内部测试通过 | Codex + Windows CI |
| T65 | ImageRecord 与 ImageIndex 模型 | T09,T28,T30,T64 | `src/images/manager-model.ts`、`manager-index-store.ts` | 版本、字段、状态、稳定 ID 和 Profile 串属校验成立 | 内部测试通过 | Codex 内部 |
| T66 | 首次、增量和手动重新扫描 | T64,T65 | `src/images/manager-scanner.ts`、`tests/images/manager-domain.test.ts` | 首次、增量、手动重扫和单文件错误隔离成立 | 内部测试通过 | Codex + macOS/Windows CI |
| T67 | 任务与项目归属 | T09,T20,T65 | `src/images/manager-attribution.ts`、`tests/images/manager-domain.test.ts` | TaskRecord、项目、任务批次和未归类回退成立 | 内部测试通过 | Codex 内部 |
| T68 | 组合筛选 | T65,T67 | `src/images/manager-query.ts`、`tests/images/manager-domain.test.ts` | 时间/项目/类型/格式/尺寸/状态/关键词组合查询成立 | 内部测试通过 | Codex 内部 |
| T69 | 排序规则 | T68 | `src/images/manager-query.ts`、`tests/images/manager-domain.test.ts` | 默认时间倒序、稳定并列和全部规划排序成立 | 内部测试通过 | Codex 内部 |
| T70 | 项目分组与最近项目 | T67,T69 | `src/images/manager-query.ts`、`tests/images/manager-domain.test.ts` | 最近项目、项目、任务、日期和不分组成立 | 内部测试通过 | Codex 页面检查 |
| T71 | 缩略图、懒加载与大图库 | T65,T70 | `src/images/manager-thumbnail.ts`、`tests/images/manager-domain.test.ts` | 按需缩略图、懒加载、网格/列表和分页成立 | 内部测试通过 | Codex 页面检查 + Windows CI |
| T72 | 详情预览与文件操作 | T65,T71 | `src/images/manager-files.ts`、`tests/images/manager-domain.test.ts` | 详情、原图、复制路径、打开目录和导出成立，无删除 | 内部测试通过 | Codex + macOS |
| T73 | 图片状态与错误呈现 | T66,T72 | `src/images/manager-scanner.ts`、`src/manager/public/app.js` | 互斥呈现 loading/empty/error/missing/corrupt/generating/failed/success | 内部测试通过 | Codex 页面检查 |
| T74 | 图片管理本地 API 契约 | T51,T63,T68,T73 | `src/manager/server.ts`、`tests/manager/server.test.ts` | 列表、详情、扫描、索引状态、参数校验和脱敏错误成立 | 内部测试通过 | Codex + Windows CI |
| T75 | 图片管理 UI 壳外部产物接收 | T62,T74 | `design/stitch/**`、`tests/manager/ui-shell.test.ts` | Stitch 来源、映射、白名单和真实入口已登记 | 内部测试通过 | Codex 页面检查 |
| T76 | Stitch 初次接收与实例级复修门禁 | T75 | `src/manager/public/**`、三视口截图 | 初次接收、实例级复修、本地资产和响应式检查完成 | 内部测试通过 | Codex 页面检查 |
| T77 | 图片管理自动化测试与安全检查 | T63-T74 | `tests/images/**`、`tests/manager/**`、`tests/contracts/coverage.test.ts` | 单元、契约、集成、文件系统、禁止删除和敏感信息边界有证据 | 内部测试通过 | Codex 内部 |
| T78 | macOS 真机与 Windows CI 验收准备 | T71-T77 | `liran_docs/09-真机实测.md` 新增 IMG 清单 | 记录入口、步骤、成功/失败标准和证据位置；范围内实测项完成 | IMG-1–IMG-3 已有真实 Profile 页面证据；IMG-2 已补真实全组合筛选、排序/分组、分页和刷新；IMG-4 的缺失、损坏、生成中、失败和权限错误态已有隔离证据；IMG-5 隔离迁移补证完成；IMG-6 当前 0.2.13 Windows CI 已绿色 | Codex macOS + Windows CI |
| T79 | Windows x64 用户验收、避坑和状态收口 | T78 | 用户验收反馈、`docs/pitfalls/**`、追踪回写 | 保留最短验收步骤和反馈模板；本轮不执行真实 Windows 用户操作 | 范围豁免/默认通过（无真机证据） | 用户 Windows x64 |

## 2026-08-01 最新真实单图复测

- `node dist/src/cli.js doctor` 与连续两次 `node dist/src/cli.js setup` 均返回 `state=ready`，专用 Profile 保留策略仍为 `never-auto-delete`。
- 任务 `task_msajqivr_9m94mq95` 在真实 ChatGPT 页面只提交一次，按 `initializing -> generating -> image_ready -> terminal` 顺序完成 `1/1 succeeded`；原图 PNG 可解码，1122×1402，哈希与 `task.json` 一致，并已在当前 Codex 对话实时显示。
- 本次补充证明 2.2、2.3 和 3.1 的新增证据；3.5、4.2–4.4、MP-4、IMG-2–IMG-5 及 Windows x64 用户验收仍未完成，不改变整体 T40/T41 门禁状态。

## 2026-08-02 受控恢复与浏览器生命周期补录

- 受控恢复、handoff 和结构诊断定向测试全部通过；证据与边界已记录在 [[09-真机实测]]，不替代真实网页中断或真实登录失效接管。
- MP-4 使用一次性临时 Profile 的真实 Chrome 完成 BrowserLease、普通/任务标签页和关闭清理检查；真实用户 Profile 未触碰。T40 仍因 3.5、真实 4.2/4.3、部分 IMG 和 Windows 用户验收未收口。

## 2026-08-02 真实 Profile 图片管理补录

- 魏邦真实图库加载 27 条，默认时间倒序；失败筛选返回 11 条，详情和导出 PNG 可解码。切换到里燃后为 0 条且无串图，页面没有图片删除能力。
- IMG-1 和 IMG-3 可记真实页面通过；IMG-2 仅证明默认排序与失败筛选，IMG-4 仅证明失败/空态，IMG-5 仍待安全的迁移后重新索引证据。

## 2026-08-02 隔离图片管理状态与迁移补录

- 两个一次性 Profile 的本地真实管理页面补齐 IMG-2 全组合唯一命中、IMG-4 生成中/失败/缺失/损坏/无结果和 IMG-5 迁移后重新加载与双向零串图；真实用户 Profile 未触碰。
- 发现筛选后的 `total=0` 被误当成 Profile 空库。新增 IMG-4 失败测试后最小修复空态判断，版本按协议从 `0.2.0` 递增到 `0.2.1`，CHANGELOG 已记录；不执行 Tag、Release 或 T42。
- 版本核对发现 CLI 仍硬编码 `0.1.1`；更新失败测试后改为从 `package.json` 读取，目标测试 3/3 通过且 `node dist/src/cli.js --version` 输出 `0.2.1`。中间提交 `a57606f` 的 Windows run `30755125640` / job `91515857204` 已绿色，但最终证据仍须等待包含本修复的新提交。
- 权限/扫描失败没有通过破坏 macOS 文件权限强行触发，仅保留自动化错误态证据；Windows x64 IMG-7 和 6.1–6.5 继续待用户验收。

## 2026-08-03 会员检测与连续修改增量

- 真实网页检测：魏邦专用 Profile 返回 `logged_in / plus` 并保持唯一 active；里燃返回 `needs_login`，启用请求返回 `LOGIN_REQUIRED`，未影响魏邦。检测后两次后台检查均释放专用 Chrome 进程和 lease 锁。
- T48 修复：`src/browser/membership.ts` 读取可见 `[role=button]`、`[role=menuitem]` 等账户入口的 `aria-label`/`title`，支持本地化名称与会员等级无空格连接；生图控件未完成 hydration 时返回未知，不将已确认的 Plus/Pro/Go 错判为不合格。`src/manager/server.ts` 在检测前等待稳定 composer。新增会员回归 6/6、管理服务回归 7/7；0.2.6 提交 `f7cf134a2bd7c0d410b31dd154ba98109d949b6f` 的 Windows CI run `30821377390` 已绿色。
- 真实连续修改：`task_msda8mwl_igzxgv5i` 首次确认不确定后只读确认网页已经提交，再复用原会话和助手回合恢复，输出 `5-_r_cg_.png`，`image_ready` 早于 terminal，未重复提交。该证据已同步到 [[09-真机实测]]。
- 页面检查：`.playwright/real-image-manager-desktop.png`、`.playwright/real-image-manager-tablet.png`、`.playwright/real-image-manager-mobile.png` 保存三视口脱敏截图；未选 Profile 无图片请求，魏邦 27 条、里燃 0 条、失败筛选 11 条、导出 PNG 可解码，页面无图片删除入口。

## 2026-08-03 Windows 夹具排队时序回归

- T39 失败证据：提交 `8f8d342` 的 Windows Actions run `30827418540` / job `91732507425` 仅 T37 `queued -> progressive images -> complete` 失败，122/123 测试通过；失败断言为 `observed.includes("queued")`。
- 根因：夹具的 `queueDelay=3000` 只延迟 `generating` 状态，图片仍按 `80 * i` 毫秒创建；Windows 调度下图片先出现并覆盖 `queued`，采样可能直接错过排队态。
- 修复动作：图片创建统一从 `queueDelay + 80 * i` 开始；T37 在提交确认后 500ms 断言首张图片尚未出现，保留 `queued -> generating -> image_ready -> complete` 状态顺序，不修改生产网页流程。
- 本机复测：目标 T37、`npm ci`、typecheck、全量测试 123/123、集成测试 12/12、build、官方 npm registry audit（0 vulnerabilities）和 `git diff --check -- .` 均通过；修复提交后的 Windows Actions 绿色结果待推送后补录。

## 2026-08-03 真实提交前中断安全节点补录

- 步骤编号：4.2（提交前安全节点）。在已登录且唯一启用的魏邦专用 Profile 上启动一次真实单图任务 `task_msdc213z_k2pwi2ti`，任务目录为 `/tmp/gwi-real-pre-submit-continue/gpt-web-images/2026-08-03/task_msdc213z_k2pwi2ti/`；在专用 Chrome 已启动但网页提交准备尚未建立 `attemptId` 时中止 CLI。
- 中断时 `task.json` 的 `state=submitting`，`submission.attemptId=null`、`clickedAt=null`、`confirmedAt=null`，`chatUrl=null`，`responseAnchor=null`，`results=[]`，`finishedAt=null`；因此没有提交确认、助手回合或可认领结果。
- 随后执行 `node dist/src/cli.js resume --task-id task_msdc213z_k2pwi2ti --output-dir /tmp/gwi-real-pre-submit-continue`，真实输出为 `{"action":"resume_before_submit","maySubmit":true,"reason":"no_attempt"}`，未打开原会话、未执行网页写操作、未重复提交。
- 中止后的专用 Chrome 进程已核对不存在；仅由已退出进程遗留的项目锁通过归属与 PID 校验后清理，`doctor --json` 仍报告专用 Profile 可写且 `retentionPolicy=never-auto-delete`。未删除、迁移或覆盖 Profile 内容，未读取认证数据。
- 结论：真实提交前中断分支补证通过；该证据只覆盖“无提交证据时安全停留”，不替代真实登录失效接管（4.3）、真实网页结构异常（4.4）或 Windows x64 用户验收。

## 2026-08-04 平板导航样式回归

- 三视口页面检查发现 768×900 顶部导航文字逐字竖排；DOM 证据显示导航按钮宽约 70–98px、高 118px，品牌区和工具区共同挤压导航区。页面无横向溢出、控制台无业务错误、网络失败为 0。
- 已按 TDD 先加入平板导航回归测试并确认失败；随后仅修改局部响应式 CSS 并完成三视口复测。1280×1024、768×900、390×844 均无横向溢出，768px 导航按钮保持 38px 高和单行文字，刷新按钮为 40×40；控制台错误和网络失败均为 0。证据截图为 `.playwright/manager-desktop-2026-08-04-fixed.png`、`.playwright/manager-tablet-2026-08-04-fixed.png`、`.playwright/manager-mobile-2026-08-04-fixed.png`，T76 页面样式检查通过。业务逻辑、真实 Profile 和图片数据不在本次修复范围。

## 2026-08-04 0.2.7 Windows CI 复验

- 提交 `5c4257baad14586384d260022f1b1bdfd82e57db` 推送后，Windows CI run `30833579612` / job `91753183805` 为 `completed/success`；依赖、Chrome、typecheck、全量合同/安全/安装/夹具测试和 build step 摘要均成功。
- 公开 job 页面要求登录才能查看原始日志，匿名 API 返回 403；因此只记录可验证的 run/job/step 摘要，不声称已读取完整逐行日志。T39/MP-7/IMG-6 的 CI 证据更新，Windows x64 用户验收仍为待用户验收。

## 2026-08-04 0.2.8 Windows hydration 夹具修复

- 失败证据：文档提交 `40bf0b7` 对应 Windows Actions run `30834139133` / job `91755062305` 失败在 T48 `manager eligibility waits for the hydrated composer before reading plan signals`。公开页面和匿名 API 只能提供失败 step/测试标识（页面要求登录、日志 API 返回 403），因此不把未读取的原始逐行日志写成已读取。
- 根因判断：T48 夹具在页面延迟 150ms 注入 composer 后，测试只给 `readBrowserEligibility` 2 秒；Windows 并行 Chrome fixture 负载下该观察预算不稳定。生产默认资格检查仍为 15 秒，本次不放宽生产门禁。
- 最小修复：`tests/manager/server.test.ts` 将该测试预算改为 5 秒，并保留延迟 hydration 断言；本机定向测试 `node --test dist/tests/manager/server.test.js` 为 7/7 通过。版本按行为调整协议递增到 0.2.8，CHANGELOG 已同步。
- 最新证据：修复提交 `b8c4e5dcbf9bfd416662794a222e657f86532ead` 对应 Windows Actions run `30837899530` / job `91767462635` 为 `completed/success`；已通过 GitHub Actions 连接器读取完整 job 日志，`npm ci`（0 vulnerabilities）、typecheck、全量测试 124/124（0 fail）和 build 均成功。旧 run `30834139133` 未复用。
- 状态：T39/MP-7/IMG-6 的当前代码 CI 证据已更新；Windows x64 用户验收和 T42 仍未执行。

### 2026-08-04 中断任务恢复复测未通过

- 任务 `task_msdjxoti_el12dplo` 在此前工具中断时已经确认网页提交，但未落盘稳定会话 URL。复测前确认项目专用 Chrome 不在运行，随后执行 `node dist/src/cli.js resume --task-id task_msdjxoti_el12dplo`，未执行第二次网页提交。
- 真实结果为 `recovering -> result_uncertain`，错误 `PAGE_STRUCTURE_CHANGED: assistant turn`，结果仍为 `0/1`，无 `image_ready` 和原图文件。任务的临时 `WEB:` 会话地址不能作为恢复锚点；不能把本次写成真实生图成功。
- 处理：按安全门禁保留不重提的 `result_uncertain`；新建唯一测试任务验证稳定会话、下载、解码和逐张回显。该问题不删除或覆盖任何真实 Profile 数据。

### 2026-08-04 稳定会话修复与真实单图复测

- 修复提交后中断窗口的 URL 边界：`WEB:` 临时地址不再写入响应恢复锚点，`auditRecovery` 只允许真实 ChatGPT UUID 会话；新增 adapter/recovery 回归测试，版本递增到 0.2.9。
- 真实任务 `task_mseabjnw_dnvoktpa` 在魏邦专用 Profile 上后台完成 `1/1 succeeded`；`image_ready` seq 4 早于 terminal seq 5，PNG 原图 1254×1254、805772 bytes，绝对路径和 SHA-256 已写入真机文档并在当前对话回显。
- 任务收尾后专用 Chrome 子进程、Profile 锁、BrowserLease 均为 0，Profile 目录和 `never-auto-delete` 标记未被删除或重建。真实 4.3/4.4、Windows x64 用户验收和 T42 仍未完成。

### 2026-08-04 macOS 台前调度窗口隐藏修复

- 真实失败：旧 `--start-minimized` 在 macOS 仍报告普通窗口；CDP `Browser.setWindowBounds` 对 macOS 窗口状态无效。该问题可能让后台任务影响用户操作。
- 最小修复：目标导航后对当前页面设置随机 UUID 标题，通过 `osascript` 仅匹配该标签页所在窗口并设置 `visible=false`，恢复标题；定位失败时关闭专用进程并释放锁。Windows/其他平台保留 CDP 最小化。
- 回归与真机：隔离集成测试 13/13；真实魏邦 Profile 无提交窗口 `matched=true, visible=false`，项目 Chrome/锁/lease 均释放。版本递增至 0.2.10。

### 2026-08-04 0.2.11 增量收口探针

- `src/browser/profile.ts` 新增 macOS 标题定位有限轮询；先写回归测试并确认旧实现失败，再修复。浏览器单测 10/10、管理 UI 单测 8/8、Chrome 集成 13/13 通过。
- 真实里燃 Profile 后台检测返回 `LOGIN_REQUIRED`，同 Profile 可视 handoff 计划保持 `attemptId` 且 `resumeWithoutSubmit=true`；未输入凭据，认证后继续任务仍保持待人工/凭据条件，不写成完整 4.3 通过。
- 真实魏邦 ChatGPT 页面 composer 受控移除后进入 `PAGE_STRUCTURE_CHANGED: composer`，诊断脱敏通过；这是受控真实页面证据，不替代自然网页结构改版。
- 隔离临时图片根权限拒绝返回 `issues[0].code=PERMISSION_DENIED`，UI 现在显示权限提示而非空 Profile；真实 Profile 未修改。
- 0.2.11 最终 Windows CI、完整内部门禁和本轮提交仍待完成；T41 按目标暂不执行并标记用户指定默认通过，T42 未执行。

### 2026-08-04 0.2.12 启动状态恢复

- 真实注册表只读检查发现魏邦记录为 `browserStatus=open`，但没有项目专用 Chrome、Profile 锁或 BrowserLease；普通 Chrome 进程未归属于项目，不能手工改真实注册表。
- 先在 `tests/manager/server.test.ts` 增加重启恢复回归并确认旧实现红灯，随后在 `BrowserLease.liveStatus()` 和管理服务启动阶段加入最小修复：只清理没有存活 lease 的陈旧 `open` 状态，保留活动 lease 对应 Profile，不改动 `taskBusy` 或 Profile 内容。
- 定向 `node --test dist/tests/manager/server.test.js` 结果为 8/8；版本按行为调整协议递增到 0.2.12，完整内部门禁、最终提交和当前提交 Windows CI 仍待完成。

### 2026-08-04 0.2.12 macOS 安装与后台复用复核

- `npm run install:user` 返回 `action=upgraded`，目标为用户级 `.codex/skills/gpt-web-image`；未覆盖其他 Skill。`node dist/src/cli.js doctor --json` 检查到正式 Chrome、专用 Profile、输出目录均可用，Profile 保留策略为 `never-auto-delete`。
- `node dist/src/cli.js setup` 返回 `state=ready`，未读取认证数据。管理服务对真实魏邦 Profile 连续两次执行资格检查，均返回 `logged_in / plus`、`browserStatus=closed`；每次检查结束后项目专用 Chrome、Profile 锁和 BrowserLease 均为 0。
- 本组证据覆盖当前版本的 Skill 安装、登录持久化和后台复用；真实登录失效后重新认证继续任务、自然网页结构改版和 Windows CI 仍按门禁保持边界状态。

### 2026-08-04 0.2.12 最终 CI 与测试竞态修复

- 当前提交 `2f506f3946fb43c4dfc8911ceb8059884e54553c` 的 Windows Actions run `30890911224` / job `91932493250` 已 `completed/success`，完整日志确认 Windows Server 2025、Node 22.23.1、npm 10.9.8、`npm ci` 0 vulnerabilities、Chrome、typecheck、130/130 测试和 build 均通过。
- 本机全量门禁首次并行运行出现 130 项中 129 通过、唯一失败为 macOS AppleScript `Google Chrome` `-600`；单独集成套件和串行全量均通过，根因是多个测试文件同时启动/关闭 headless 与正式 Chrome 的应用生命周期竞争。
- 最小修复：`package.json` 的 `test`、`test:unit`、`test:integration` 统一加入 `node --test --test-concurrency=1`；新增跨平台避坑条目，未改变生产业务逻辑。修复后本机全量 130/130、集成 13/13 通过。
- 状态：T39/MP-7/IMG-6 当前提交证据已更新；T40 的 4.3 认证后继续、4.4 自然网页改版和真实用户目录破坏性操作继续保持边界状态；T41 按目标暂不执行，T42 禁止执行。

### 2026-08-04 b2f2ecd 修复提交 CI 收口

- `package.json` 测试并行约束和本轮文档/避坑追加已提交为 `b2f2ecdc00221776dcb65493f300b1bd2a106331` 并推送 `origin/codex/manager-internal-closure`。
- 最新 Windows Actions run `30892675106` / job `91938185693` 为 `completed/success`；完整日志确认 `npm ci` 0 vulnerabilities、Chrome、typecheck、130/130 测试和 build 通过。
- T39/IMG-6 的最新提交证据已闭合；macOS 4.3/4.4 和真实用户 Profile 破坏性边界不因 CI 绿色而改变，T41 按目标暂不执行，T42 禁止执行。

### 2026-08-04 提交后中断安全复测回写

- 魏邦真实 Plus Profile 的两个一次性任务在网页提交确认后被中断，任务均保存 `submitted`、无稳定 `chatUrl`、无图片结果；`resume` 返回 `result_uncertain` 且 `maySubmit=false`，未重复提交，Chrome/锁/lease 均已释放。
- 该结果只证明缺少稳定会话锚点时的安全拒绝路径，不改变 4.3 认证后继续和 4.4 自然网页改版的未验证边界。

### 2026-08-04 43321b5 最终 Windows CI

- 最新文档提交 `43321b571e6c6057331ab51f543a855d34a81ea6` 已推送；Windows Actions run `30894814644` / job `91945099220` 为 `completed/success`。
- 完整日志确认 `npm ci` 0 vulnerabilities、Chrome 探测、typecheck、全量测试 `130/130` 和 build 通过；该 CI 不证明真实 ChatGPT、macOS 真机或 Windows x64 用户验收。

### 2026-08-04 真实提交前安全节点复测

- 任务 `task_msefwaa5_xvz7ywl4` 在 `initializing` 写盘后被终止，提交字段、会话锚点和结果均为空；`resume` 返回 `resume_before_submit` / `maySubmit=true`，未重复提交。
- 已核对 Chrome、Profile 锁、BrowserLease 均释放，doctor 仍通过；4.2 的提交前安全节点证据现已补齐。

### 2026-08-04 BrowserLease 并发错误映射修复

- 真实管理服务复核时发现同一 Profile 的并发 `/check` 请求中，租约冲突被 `safeError()` 降级为通用 500；该错误不会绕过 BrowserLease，但客户端无法识别冲突。
- 按 TDD 新增 `manager reports a concurrent BrowserLease conflict as a 409` 回归：修复前为 200 + 500，修复后管理服务定向测试 9/9，通过 200 + 409 且错误码为 `BROWSER_LEASED`。
- 最小修复仅更新 `src/manager/server.ts` 的已知错误白名单；版本递增到 0.2.13，CHANGELOG 和避坑知识库已同步。未触碰真实 Profile、任务文件或认证数据。
- 本次复核同时确认 `npm test` 130/130、集成测试 13/13、typecheck、build、官方 npm audit 0 vulnerabilities 和 `git diff --check` 在修复前通过；修复后需再次执行全量门禁并等待最新提交 Windows CI。

### 2026-08-04 0.2.13 修复后门禁与真实并发复测

- 修复后 `npm ci`、typecheck、全量测试 131/131、集成测试 13/13、build、官方 npm audit 0 vulnerabilities 和 `git diff --check -- .` 均通过。
- 真实管理服务同一未登录 Profile 的并发 `/check` 返回一条正常 `needs_login` 和一条 `HTTP 409 / BROWSER_LEASED`；未启动第二个项目浏览器，收尾时 Chrome、锁和 BrowserLease 均释放。
- 当前仍保持 4.3 认证后继续、4.4 自然网页改版、真实用户目录破坏性管理操作和 Windows x64 用户验收的证据边界；等待 0.2.13 最终 Windows CI。

### 2026-08-04 用户级 Skill 副本清理

- 按用户要求扫描 `~/.codex/skills`、`~/.agents/skills`、Codex 插件缓存和项目目录中的 `gpt-web-image` 相关路径。发现 `~/.codex/skills` 下 10 个 `gpt-web-image.backup-*` 历史副本，每个仅包含旧 `SKILL.md`、`agents/openai.yaml` 和安装元数据。
- 已删除上述 10 个历史副本；保留正式用户级 Skill `~/.codex/skills/gpt-web-image` 和项目源码 Skill `.agents/skills/gpt-web-image`。未发现其他同名备份或重复用户级 Skill。
- 删除后校验：用户级 `SKILL.md` 数量为 1；正式用户级与项目源码 `SKILL.md` 的 SHA-256 一致；正式 Skill 的元数据和入口文件完整。专用 Chrome Profile、图片、任务数据和业务代码未修改。
- 本次仅清理本机安装备份并更新记录，不改变产品行为、不递增版本、不执行 T42 发布。

### 2026-08-04 全局旧插件备份清理

- 复核 `~/.codex/plugins/` 后发现 41 个明确命名为 `codex-image-bridge.backup-*` 的旧插件目录；未发现运行中的进程引用这些目录。
- 已删除上述 41 个旧插件备份，仅保留正式插件 `~/.codex/plugins/codex-image-bridge`。当前项目的正式用户级 `gpt-web-image` Skill、项目源码 Skill、专用 Chrome Profile、图片和任务数据未修改。
- 本次不处理 Codex 系统配置/认证的 `.bak` 文件、Playwright/Chrome 的 `LOG.old`、项目中的 `backup.ts` 或正式插件运行数据；它们不是该 Skill 的重复安装副本，避免误删用户环境数据。
- 删除后复核备份目录数量为 0，正式插件目录和项目 Skill 入口仍存在；本次仅做本机副本清理，不改变产品行为、不递增版本、不执行 T42 发布。

### 2026-08-05 Profile 管理修复与本轮真机收口

- 修复范围：Profile 列表中的启用、打开浏览器、检测状态和关闭浏览器语义分离；启用前检查目录归属、登录和 Plus/Pro/Go 资格，切换时关闭旧的项目浏览器并保持唯一 `activeProfileId`；检测只更新账号/会员/Chrome 状态，不改变 active；打开/关闭只操作项目拥有的专用 Chrome。
- 增加 `pathStatus` 归属健康信息和操作前路径复核；关闭过程持久化为 `closing`，异常时释放 BrowserLease/锁并回写 `unknown`；扫描与创建/导入在提交事务内再次去重，避免并发重复注册。
- UI 修复包含 Profile 切换弹窗、统一 Toast（成功/失败/警告/提示、自动关闭、手动关闭、`aria-live`）、创建/导入表单联动和 768px 卡片布局；未添加图片删除入口。
- 本机最终门禁：`npm ci`、`npm run typecheck`、`npm test`（136/136）、`npm run test:integration`（13/13）、`npm run build`、官方 registry `npm audit --audit-level=high`（0 vulnerabilities）和 `git diff --check -- .` 均通过。
- 隔离 macOS 真机链路：真实页面完成创建、合法专用 Profile 导入、普通 Chrome 目录拒绝、备注编辑、资格检查失败保护、唯一 active 切换、正式 Chrome 打开/关闭；关闭后项目 Chrome、Profile 锁和 BrowserLease 均为 0，破坏性动作未触碰真实 Profile。
- 页面样式检查：1280×1024、768×900、390×844 均无横向溢出、控制台错误和失败请求；证据截图为 `.playwright/manager-isolated-desktop.png`、`.playwright/manager-isolated-tablet.png`、`.playwright/manager-isolated-mobile.png`。
- 真实 macOS 魏邦 Profile 只读复核：`doctor --json` 与管理服务检测均报告正式 Chrome、固定 Profile 路径、`pathStatus=ok`、`logged_in / plus`、唯一 active；里燃保持 `needs_login` 且不能替换 active。真实认证数据、Profile 删除/迁移/覆盖均未执行。
- Windows CI 收口：提交 `81613228a082a10930b01212dfcedd6d17e2ccb8` 对应 run `30943199641` / job `92106538196` 已 `completed/success`；完整日志确认 Windows Server 2025、Node 22.23.1、npm ci（0 vulnerabilities）、Chrome 探测、typecheck、136/136 测试和 build 均通过。
- 当前状态：T40 范围内管理页面、隔离创建/导入和 macOS Profile 检测/生命周期已完成；T39 Windows CI 已完成；4.3、4.4 和 Windows x64 用户真机仍按用户范围不执行；T42 未执行。
