# 数量记忆与补图

上级：[[_04-任务编排]]
下级：无
依赖：[[配置加载与默认值]]、[[新会话与连续修改]]、[[状态机与迁移]]

---

## 场景

用户省略数量、明确要求 1–10 张，或 ChatGPT 返回数量少于目标。

## 触发

请求解析、生成活动结束且已验证结果少于目标。

## 逻辑

首次省略数量使用 1；明确数量保存为新的全局默认。少图时在原会话请求缺少数量，最多 3 轮，累计使用已校验且未重复的结果计数。

## 状态 / 边界

额外返回图片不改变默认数量；补图不能新建会话。达到上限后以部分成功收口并报告缺少数。
