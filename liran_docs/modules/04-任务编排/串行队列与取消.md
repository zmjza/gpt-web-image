# 串行队列与取消

上级：[[_04-任务编排]]
下级：无
依赖：[[TaskRecord与任务ID]]、[[单实例锁与残留进程]]

---

## 场景

同一 Profile 收到多个任务，或用户取消正在排队/执行的任务。

## 触发

任务创建、前一任务结束、取消命令或进程退出信号。

## 逻辑

任务按创建顺序排队，一次只授予一个执行权；状态显示队列位置。取消设置持久化信号，停止后续提交/补图，安全关闭当前操作并保留合格结果。

## 状态 / 边界

取消必须幂等。已触发下载的文件需完成或安全清除临时文件；不得把取消报告为失败或删除已成功图片。
