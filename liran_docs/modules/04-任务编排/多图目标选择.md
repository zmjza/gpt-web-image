# 多图目标选择

上级：[[_04-任务编排]]
下级：无
依赖：[[逐张实时回显]]、[[格式校验与去重]]

---

## 场景

用户要求继续修改，但上轮有多张图片且未明确是哪一张。

## 触发

edit/refine 输入无法唯一解析 resultId，且也没有明确附件或“全部修改”。

## 逻辑

按稳定顺序编号已验证 ImageResult，回显预览和编号，等待用户选择；单张自动选中，明确“第 N 张/最后一张/全部”直接解析。

## 状态 / 边界

等待选择期间不得提交。用户提供附件时以文件哈希和路径匹配；“全部”会形成明确批量计划，不能默认推断。
