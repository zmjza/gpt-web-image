# 默认目录与自动扫描

上级：[[_11-多Profile管理]]
依赖：[[系统目录与路径规范]]、[[Profile注册表与生命周期]]

## 目标

解析 macOS/Windows 默认根目录，只扫描受控层级和项目归属标记。扫描结果必须区分已登记、已发现、跳过和冲突。

## 规则

- 不扫描全盘，不跟随未确认符号链接，不识别日常 Chrome。
- 旧根目录在“保留”后通过注册表继续显示；新建和自动发现以新默认根目录为准。
- 默认路径修改必须先做可写、越界、冲突和占用预检。

## 任务

关联：T44、T58。验证：T-MP-02、T-MP-05、T-MP-19。
