# Codex 生图 Profile 绑定

上级：[[_11-多Profile管理]]
依赖：[[唯一启用与资格检查]]、[[单实例浏览器与任务标签页]]、[[任务编排]]

## 规则

`generate/edit/refine/resume` 在任务开始时读取唯一启用 Profile，并把 `profileId` 和规范化路径写入任务快照。切换只影响新任务；没有合格启用项时禁止网页提交，不得静默回退。

## 任务

关联：T50。验证：T-MP-17、现有任务恢复和串行队列回归测试。
