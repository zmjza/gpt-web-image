# 完整备份与恢复

上级：[[_11-多Profile管理]]
依赖：[[Profile创建导入修改删除]]、[[单实例浏览器与任务标签页]]

## 规则

备份是用户主动操作，包含完整 Chrome 登录数据，按用户确认不加密；页面必须显示敏感数据警告。浏览器和任务必须关闭，备份写入本机且不进入 Git。恢复默认创建新 Profile，不覆盖源 Profile。

## 任务

关联：T54、T55。验证：T-MP-15、T-MP-16。
