# 原图下载与临时文件

上级：[[_07-图片交付]]
下级：无
依赖：[[图片发现与稳定判定]]、[[系统目录与路径规范]]

---

## 场景

稳定图片需要获取网页提供的原始文件并安全写入本地。

## 触发

图片候选首次达到稳定状态。

## 逻辑

优先同时等待 Playwright download 事件并点击对应下载入口；不可用时读取页面已经暴露的本次原始资源。写入任务 `.partial` 临时目录，完成后交给校验器。

## 状态 / 边界

禁止使用截图作为交付。资源回退不得请求未在页面暴露的私有端点；失败保留原因并可在明确可恢复时有限重试。
