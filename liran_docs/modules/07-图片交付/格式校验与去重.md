# 格式校验与去重

上级：[[_07-图片交付]]
下级：无
依赖：[[原图下载与临时文件]]、[[TaskRecord与任务ID]]

---

## 场景

下载可能得到 HTML 错误页、空文件、损坏图片或与前一结果相同的文件。

## 触发

临时文件关闭并准备转正。

## 逻辑

读取魔数并与 MIME/扩展名核对，真实解码，验证宽高和合理字节数，计算 SHA-256；未重复则原子移动到正式路径并创建 ImageResult。

## 状态 / 边界

扩展名不能作为唯一证据。重复哈希不增加成功数或触发第二次回显；校验失败保留脱敏错误并清理临时文件。
