# UI 壳接入清单

> Stitch 静态 UI 壳已进入第一次 `/goal` 内部接线；当前入口使用真实本地 API、Profile/目录/备份/浏览器/图片状态，不再以 mock 充当运行结果。

## 当前状态

- 文本视觉方案：已确认，采用浅色本地控制中心。
- 路线：`@product-design + Stitch MCP`。
- Stitch Project：`GPT Profile Manager`。
- Project ID：`18230660193198829480`。
- Stitch Screen：`GPT Web Image - 导航易读性优化版`。
- Screen ID：`06f8eaec0ace43eb91960233146f13c9`。
- Screen 关系：一个 Stitch Screen 内包含 Profile 总览、创建/导入/编辑、默认目录与迁移、Profile 详情与安全操作四个逻辑 Screen。
- 目标画布：2560×2048，桌面端；Stitch 截图下载尺寸为 512×410 预览图。
- 初次接收检查：可以进入实例级细节复修。
- 图片管理业务接线：已完成内部实现，包含先选 Profile、单 Profile 请求隔离、扫描、筛选、排序、分组、分页、详情和不可用状态。
- 运行时数据：`src/manager/public/app.js` 调用同源 `/api/**`；`mock-data.js` 仅保留为空兼容模块。
- 前端资产：Tailwind 和 Font Awesome 均本地构建/复制，运行时不请求 CDN。
- 阶段状态：全量命令、真实同源 API 接线和 1280×1024、768×900、390×844 三视口复核已通过；Profile 管理范围已完成 macOS 真实只读与隔离页面验收，Windows CI 已通过；Windows x64 用户真机和明确豁免项仍不伪造为通过。

## 视觉事实来源

1. 第一事实来源：Stitch 下载的 HTML。
2. 第二事实来源：Stitch 下载的代码、CSS、资源和设计规范。
3. 第三事实来源：Stitch 截图，只用于补充校对。

能从 HTML 确认的结构、尺寸、间距、字体、class 和层级不得靠截图猜，也不得被项目通用化样式抹平。

## 下载与映射

| Stitch 实体 | 计划下载目录 | 真实前端目标 |
|---|---|---|
| Project `18230660193198829480` | `design/stitch/18230660193198829480/` | 仅作原始事实来源 |
| Screen `06f8eaec0ace43eb91960233146f13c9` | `design/stitch/18230660193198829480/06f8eaec0ace43eb91960233146f13c9/` | `src/manager/public/` |

## 允许编辑文件

- `src/manager/public/index.html`：真实页面入口和 Stitch 结构承接。
- `src/manager/public/styles.css`：只作用于 `[data-manager-shell]` 的局部样式。
- `src/manager/public/app.js`：静态交互、页面/弹窗切换和后续接线锚点。
- `src/manager/public/mock-data.js`：未使用的空兼容导出，不得作为运行时回退数据。
- `src/manager/public/ui-contracts.js`：JSDoc 类型和真实 API 接线边界。
- `src/manager/public/assets/background.png`：Stitch 原始页面引用的背景位图本地副本。
- `src/manager/public/assets/image-cyberpunk.jpg`、`image-waves.jpg`：图片管理 Screen 的 Stitch 原始示例资源本地副本。
- `scripts/preview-manager.mjs`：启动只监听 `127.0.0.1` 的真实管理服务预览入口。
- `package.json`：只允许维护 `preview:manager` 命令。
- `tests/manager/ui-shell.test.ts`：UI 壳结构和安全边界测试。
- `design/stitch/18230660193198829480/**`：Stitch 原始下载产物，不做自由改写。
- 本清单、单壳清单、`03-索引.md`、`04-开发追踪.md`：记录事实、映射和阶段状态。

## 禁止编辑范围

- 不修改现有 `src/cli.ts`、`src/browser/**`、`src/tasks/**`、`src/profiles/**` 或规划 API。
- 不在 UI 层读取或展示密码、Cookie、Token、验证码和完整认证日志；真实接口由本地管理服务负责脱敏接线。
- 不新增前端框架，不替换全局主题，不修改公共组件或 CSS reset。
- 不把 Stitch 静态数据描述为真实 Profile 数据。
- 不执行真实删除、迁移、备份、恢复或浏览器关闭操作。

## 查看入口

- 命令：`npm run preview:manager`。
- 默认地址：`http://127.0.0.1:4173`。
- 当前入口已用于真实本地管理服务和 UI 壳验证；服务仅监听 `127.0.0.1`。

## 2026-08-05 管理页内部接线与样式验收

- Profile 列表已区分“启用”“检测状态”“打开浏览器”“关闭浏览器”：启用只切换唯一 active，检测只更新资格/Chrome 状态，打开/关闭只控制项目拥有的专用 Chrome。
- `pathStatus` 在列表和操作前均校验；路径不属于项目专用目录时显示明确错误，不允许启用、检测、打开或关闭。关闭按钮进入 `closing` 状态，避免重复点击和残留租约。
- 顶部 Profile 切换弹窗、统一 Toast 和创建/导入表单均使用真实同源 API；图片管理仍要求先选择 Profile，且没有图片删除入口。
- 隔离页面 E2E 已覆盖创建、合法导入、普通目录拒绝、编辑、唯一 active、Chrome 打开/关闭和资源清理；三视口截图和无溢出/无错误检查通过，证据见 `09-真机实测.md`。
- 当前提交的 Windows CI 已通过：run `30944073691` / job `92109518018`；UI 契约、Profile 生命周期、路径、安装和 Chrome 夹具均在 `windows-latest` 上通过，不能替代真实 Windows 用户验收。

## 已完成门禁记录

1. 下载并记录 Stitch HTML、截图、资源和元数据。
2. 在单壳清单完成控件规格盘点。
3. 按 HTML 一比一落到真实前端文件并保留静态数据。
4. 初次接收结果只能是“可以进入实例级细节复修”或“初次承接受阻”。
5. 强制实例级细节复修已完成后，进入第一次 `/goal` 业务接线并完成内部验证。

## 图片管理页面接入计划

当前状态：图片管理 Screen 已承接到现有 `src/manager/public/**` 的同一顶部导航壳，并已接入真实图片索引 API；内部验证、三视口样式检查和范围内 macOS/隔离管理验收已完成，剩余只保留 Windows 用户真机或明确豁免项。

### 未来接入范围

- 复用现有顶部导航，在当前控制台增加“图片管理”视图，不创建独立 prototype 或第二套壳。
- 页面文件允许继续使用 `src/manager/public/index.html`、`styles.css`、`app.js`、`mock-data.js`、`ui-contracts.js` 和 `tests/manager/ui-shell.test.ts`；实际增删以用户提供的 Stitch/HTML 产物和接入清单为准。
- 必须预留 Profile 选择、图片列表、筛选、排序、分组、预览、文件操作、loading/empty/error/no-results/generating/failed 状态以及 API/type/event 锚点。
- 页面不得直接读取认证数据；扫描、路径操作、迁移和备份通过本地 API 完成，图片管理继续禁止删除。

### 外部产物门禁

图片管理 Stitch 产物已收到并完成原始下载：

- Project：`18230660193198829480`。
- Screen：`GPT Web Image - 图片管理全功能完备版 (含状态库)`，ID `52f2dc24e0cd4878861f315a7edefa28`。
- 原始 HTML：`design/stitch/18230660193198829480/52f2dc24e0cd4878861f315a7edefa28/index.html`。
- 截图：同目录 `screenshot.png`；资源：同目录 `assets/`。
- 规格：DESKTOP，1280×2452；截图为 Stitch 服务端预览尺寸，不作为布局尺寸事实。

接收检查结论：初次接收和实例级细节复修已通过。原始产物仅作视觉事实来源，真实 API、文件索引和状态由项目实现提供。

### 图片管理实例级复修证据

- 1280×1024：图片管理导航、Profile 选择器、三张统计卡、筛选工具栏、四列图片网格、状态参考和详情弹窗均可见；页面 `scrollWidth` 与 `clientWidth` 同为 1280。
- 390×844：图片网格切为单列，Profile 选择器宽 358px；页面 `scrollWidth` 与 `clientWidth` 同为 390；详情弹窗为 390×844 且内部滚动。
- 图片资源：两个 Stitch 原始示例资源均成功解码，原始尺寸分别为 512×279 和 512×286。
- 移动导航：刷新操作收敛为 40×40 图标按钮，避免文字竖排；可见按钮最右边界为 378.59px，没有被视口裁切。
- 控制台：仅保留已知 Tailwind CDN 部署警告，无脚本异常和资源 404。
- 证据截图：`.playwright/manager-images-desktop.png`、`.playwright/manager-image-modal-desktop.png`、`.playwright/manager-images-mobile.png`、`.playwright/manager-image-modal-mobile.png`、`.playwright/manager-images-mobile-final.png`。
- 验证：`npm test` 全量通过；`git diff --check -- .` 通过。

### 2026-08-04 平板导航修复复测

- 768×900 初始检查发现顶部导航因 flex 收缩导致中文逐字换行；已在 `styles.css` 增加 768–1023px 紧凑规则、`white-space: nowrap` 和固定刷新图标区，并保留必要账户入口。
- 使用正式 Chrome 临时无登录上下文复测 1280×1024、768×900、390×844：页面均无横向溢出；768px 导航按钮为 38px 高且单行，刷新按钮 40×40；控制台错误与网络失败均为 0。
- 证据截图：`.playwright/manager-desktop-2026-08-04-fixed.png`、`.playwright/manager-tablet-2026-08-04-fixed.png`、`.playwright/manager-mobile-2026-08-04-fixed.png`。回归测试 `node --test dist/tests/manager/ui-shell.test.js` 为 7/7。

### 2026-07-31 真实管理页面/API 复核补录

- 真实回环管理服务已在 `127.0.0.1:4176` 运行并使用隔离 Playwright Chrome 复核 1280×1024、768×900、390×844 三个视口；本地页面没有请求 CDN 资源或出现业务资源 404。
- 图片管理流程：未选 Profile 时图片列表请求数为 0；选择魏邦后只携带该 `profileId` 请求图片索引，加载 27 条真实索引记录，默认按时间倒序；关键词与状态组合筛选、图片详情、原图预览、本地路径、复制路径和导出入口均已实际操作。
- 页面安全边界：正文和操作区没有“删除”能力；未执行真实 Profile 的删除、迁移、覆盖、备份或恢复；截图只保存在 `.playwright/`，不包含认证数据。
- 当前证据仍不等于 MP-2–MP-6 完整业务通过；创建/导入/备注、迁移/保留、BrowserLease、隔离删除和隔离备份恢复需要按 `09-真机实测.md` 使用临时数据根补做。

### 隔离临时数据根页面操作补录

- 通过真实页面完成创建/编辑/导入、受控 Plus 检测与唯一启用、二次确认删除、默认目录保留/迁移、完整备份及恢复为新 Profile；证据截图为 `.playwright/manager-isolated-mp-complete.png`。
- 所有破坏性动作只作用于一次性临时目录；图片删除请求返回 405。真实会员、真实 Chrome Profile 和真实 BrowserLease 不由该受控页面脚本伪造通过。

## 初次接收证据

- 预览入口：`http://127.0.0.1:4173/`。
- 1280×1024：页面可打开，`overview`、`migration`、`security`、`detail` 视图存在；导航切换可用；Profile 创建弹窗可打开。
- 390×844：页面可打开，但窄屏导航和当前启用 Profile 横幅出现文字/按钮挤压，列入实例级复修。
- 控制台：仅发现 Tailwind CDN 的生产使用警告；没有业务脚本异常。
- 证据截图：`.playwright/manager-overview-1280.png`、`.playwright/manager-overview-390.png`。
